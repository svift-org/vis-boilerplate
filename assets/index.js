SVIFT.vis.{{visname}} = (function (data, container) {
 
  // Module object
  var module = SVIFT.vis.base(data, container);
 
  module.setup = function () {
  };

  module.resize = function () {
  };

  module.timeline = {};

  return module;
 });